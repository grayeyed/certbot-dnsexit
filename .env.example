# Required
DNSEXIT_API_KEY=your_dnsexit_api_key_here
LETSENCRYPT_EMAIL=your-email@example.com

# Optional
DOMAINS=example.com,*.example.com
LOG_LEVEL=INFO
RENEWAL_INTERVAL=86400
CERTBOT_CERTONLY_EXTRA_ARGS="" # e.g., "--force-renewal --rsa-key-size 4096"
LOG_FILE="" # e.g., "/var/log/letsencrypt/certbot.log"
# User and Group IDs (PUID/PGID)
# These control the user and group ID the container runs as.
# Default: PUID=0 (root), PGID=0 (root)
# For security, it's recommended to run as a non-root user.
# To use a specific user ID and group ID (e.g., 1000:1000):
# PUID=1000
# PGID=1000
